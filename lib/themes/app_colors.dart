import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF1A237E);
  static const Color primaryDark = Color(0xFF000051);
  static const Color primaryLight = Color(0xFF534bae);

  // Secondary Colors
  static const Color secondary = Color(0xFFF44336);
  static const Color secondaryDark = Color(0xFFba000d);
  static const Color secondaryLight = Color(0xFFff7961);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFF9E9E9E);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Border Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // Icon Colors
  static const Color icon = Color(0xFF757575);

  // Card Colors
  static const Color card = Colors.white;
  static const Color cardShadow = Color(0x0A000000);

  // Button Colors
  static const Color button = primary;
  static const Color buttonDisabled = Color(0xFFE0E0E0);

  // Progress Colors
  static const Color progressBackground = Color(0xFFE0E0E0);
  static const Color progressValue = success;

  // Chatbot Colors
  static const Color chatbotUser = Color(0xFFE3F2FD);
  static const Color chatbotBot = Color(0xFFF5F5F5);

  // Dark Theme Colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkTextPrimary = Colors.white;
  static const Color darkTextSecondary = Color(0xFFB0B0B0);

  // Course Status Colors
  static const Color free = success;
  static const Color locked = error;
  static const Color comingSoon = warning;

  // Payment Status Colors
  static const Color pending = Color(0xFFFF9800);
  static const Color verified = success;
  static const Color rejected = error;

  // Subscription Status Colors
  static const Color active = success;
  static const Color expired = error;
  static const Color cancelled = Color(0xFF757575);
}
